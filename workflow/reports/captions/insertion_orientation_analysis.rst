Insertion orientation
======================

One figure per sample, one subplot per timepoint: log-log scatter of
forward (+) vs. reverse (-) strand read counts at the same insertion site.
piggyBac inserts without strand preference, so a tight diagonal is expected;
systematic deviation from the diagonal flags a strand bias worth
investigating.
