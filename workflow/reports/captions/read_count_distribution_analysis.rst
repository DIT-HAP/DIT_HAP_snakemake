Read count distribution
========================

One page per sample: log10-scaled histograms of read counts at every
timepoint, with the hard-filtering cutoff drawn as a vertical line. Mass to
the left of the cutoff is removed before depletion scoring; a healthy library
has most of its distribution sitting comfortably to the right.
